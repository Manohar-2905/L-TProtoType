name: Keep Alive

on:
  schedule:
    - cron: '*/14 * * * *' # Every 14 minutes
  workflow_dispatch: # Allow manual trigger

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Services
        run: |
          curl -I https://aura-ai-engine.onrender.com/
          curl -I https://aura-server.onrender.com/api/ai/full-stack
          curl -I https://aura-client.onrender.com/
